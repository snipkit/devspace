name: devspace-pro
version: v0.0.1
icon: https://dev.khulnasoft.com/assets/devspace.svg
home: https://dev.khulnasoft.com
description: DevSpace Pro
options:
  LOFT_CONFIG:
    global: true
    hidden: true
    required: true
    default: "${PROVIDER_FOLDER}/loft-config.json"
exec:
  daemon:
    start: ${DEVSPACE} pro daemon start
    status: ${DEVSPACE} pro daemon status
